module.exports = {
    presets: [
        ["minify", {
            builtIns: false,
            evaluate: true,
            mangle: true,
        }],
    ],
    comments: true,
    shouldPrintComment: (comment) => /licen[sc]e|copyright|@preserve|^!/i.test(comment),
}
